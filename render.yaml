services:
  - type: web
    name: nfa-dfa-visualizer
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y graphviz
      pip install -r requirements.txt
    startCommand: python backend/app.py